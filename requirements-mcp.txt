# Official Model Context Protocol Python SDK v2.
mcp>=2,<3
# Exact pin (not a range): the T033 connection-bound ("F2") egress path in
# course_compiler/mcp_file_ingress.py is built directly on this exact
# version's httpcore.NetworkBackend/NetworkStream public extension-point
# API, which was inspected and security-reviewed against 1.0.9 specifically
# A future httpcore release could change that surface in ways
# requiring its own re-review before this ingress path relies on it.
httpcore==1.0.9
