# Semantic review protocol

Return exactly one review verdict. Start with `# Review verdict`.

For an accepted course, write `No corrections required.`. Otherwise write
`## Corrections required` followed by one `- lN` item for every affected
lecture. You may add `## Findings` afterwards; findings are advisory only.
