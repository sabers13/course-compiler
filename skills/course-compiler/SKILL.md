---
name: course-compiler
description: Build or resume a source-grounded exam-preparation course using the Course Compiler app's semantic prompt and current evidence. Do not activate for isolated explanations or summaries.
---

# Course Compiler

Read `prompts/course-authoring-v2.md` as the canonical versioned semantic product
contract. The app includes that exact contract in each prompt. Do not weaken its
mandatory source fidelity, coverage, priority, pedagogy, math or exercise obligations.
Read `references/coarse-workflow.md` for transport and recovery.

Use **Continue in ChatGPT** / **Resume in ChatGPT** in the local app. The owner
copies the current semantic prompt and attaches its current evidence. Use only that
prompt, course guidance, exact approved plan, active lecture and evidence. Never
rely on prior conversation memory for course authority.

Return semantic Markdown only: the assessment sections requested by the prompt,
the rich lecture plan, or the single active lecture. The app owns machine identity,
ordering, revisions, hashes and persistence. Never construct a result envelope,
Base64 payload or application protocol. Owner plan approval happens in the app;
never claim approval on the owner's behalf. You must not attempt localhost fetches.
No automatic subscription/API connection is implied by this browser-mediated workflow.
